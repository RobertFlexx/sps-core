#!/sbin/openrc-run
description="DHCP client daemon"
command="/usr/sbin/dhcpcd"
command_args="--nobackground"
command_background="yes"
pidfile="/run/dhcpcd.pid"

depend() {
    use net
    after net
}
